name: aws-secretmanager-project

on:
  push:
    paths:
      - "aws-secretmanager-project/**"
    branches: [ main ]
  pull_request:
    paths:
      - "aws-secretmanager-project/**"
    types:
      - opened
      - synchronize
      - reopened

jobs:
  build:
    name: Run Unit & Integration Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: "./aws-secretmanager-project"
    steps:
      - uses: actions/checkout@v3
        with:
          fetch-depth: 0  # Shallow clones should be disabled for a better relevancy of analysis

      - name: Set up JDK 17
        uses: actions/setup-java@v3.6.0
        with:
          java-version: 17
          distribution: 'zulu'
          cache: 'maven'
      - name: Start up localstack via Docker Compose
        run: |
          cd docker
          docker compose up -d localstack
          sleep 5
          docker ps -a
      - name: Build and analyze
        run: ./mvnw clean verify